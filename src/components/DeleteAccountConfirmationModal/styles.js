const styles = theme => ({
    accDetailsText: {
        display: 'flex',
        alignItems: 'center',
        margin: '10px 0',
    },
    accDetailsIcon: {
        marginRight: 10
    },
    accTitleText: {
        color: theme.palette.primary.main,
    }
});

export default styles;